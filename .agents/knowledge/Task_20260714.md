# Real-time Warehouse Inventory Mapping System - Task List

- `[ ]` **Phase 1: Camera Input Framework (`src/camera/stream_manager.py`)**
  - `[ ]` Implement `MobileHubMode` (Wi-Fi/RTSP stream from S24 Ultra)
  - `[ ]` Implement `SDICaptureMode` (Blackmagic Capture input)
  - `[ ]` Implement `LocalEdgeMode` (Direct USB/Webcam input for M90q)
  - `[ ]` Add OpenCV Camera Calibration (Undistort) for Insta360 wide-angle lenses

- `[ ]` **Phase 2: Localization Module (`src/localization/`)**
  - `[ ]` Implement `aruco_slam.py` (Absolute 6DoF positioning via ArUco markers)
  - `[ ]` Implement `visual_slam.py` (Feature-based Visual Odometry)
  - `[ ]` Integrate VIO (Visual-Inertial Odometry) using sensor fusion (IMU + Camera)

- `[ ]` **Phase 3: Detection & Decoding Pipeline (`src/inference/detector.py`)**
  - `[ ]` Integrate YOLOv11 + ZBar ensemble logic
  - `[ ]` Add Hailo SDK (HailoRT) inference branching for Setup C (On-Device Edge AI)
  - `[ ]` Implement Ray Casting logic to calculate 3D absolute coordinates of detected barcodes

- `[ ]` **Phase 4: 2D Map Visualization Web UI**
  - `[ ]` Update `src/app.py` to broadcast JSON metadata (Coordinates + Barcode ID) via WebSockets/SSE
  - `[ ]` Create `src/static/map_ui.html` (Responsive, Glassmorphism UI)
  - `[ ]` Implement HTML5 Canvas drawing to render camera trajectory and barcode points

- `[ ]` **Phase 5: Verification & Testing**
  - `[ ]` Verify Hailo `.hef` model compilation
  - `[ ]` Compare Network Bandwidth (Setup B vs Setup C)
  - `[ ]` Final integrated walkthrough (Live test with Insta360 or MP4 fallback)
